import type { KitFixtureMetadata } from "../../shared/KitDefinition"

export const puniPushControllerFixture: KitFixtureMetadata = {
  kitId: "controller.puniPush.v1",
  fixtureId: "puni-push-controller",
  phase: "runtime-ready",
  notes: "Drives the player actor from pointer/touch drag (mouse follows the same path) with keyboard fallback.",
}
