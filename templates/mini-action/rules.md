# Mini Action Template Rules (Skeleton)

- This template defines slot contracts only; no runtime implementation is included.
- `requiredSlots` must be satisfied by selected Kits before generation.
- `optionalSlots` may be omitted without breaking template validity.
- Template files under `files/` are placeholders for future Assembler output mapping.
- Do not add generated runtime files in this phase.
